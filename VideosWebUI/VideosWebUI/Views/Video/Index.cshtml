@model IEnumerable<VideosWebUI.Models.Video>

@{
    ViewData["Title"] = "Videos";
}

<h1>Videos</h1>

<p>
    <a asp-action="Upload">Upload New Video</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>Filename</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var Video in Model)
        {
            <tr>
                <td>@Video.FileName</td>
                <td>
                    <a asp-action="Play" asp-route-fileName="@Video.FileName">Play</a>
                </td>
            </tr>
        }
    </tbody>
</table>
